<Window x:Class="Task3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Menu Height="25" VerticalAlignment="Top">
            <MenuItem Header="Файл">
                <MenuItem x:Name="LoadFile" Header="Открыть" Click="LoadFile_Click"/>
                <MenuItem x:Name="SaveFile" Header="Сохранить" Click="SaveFile_Click"/>
            </MenuItem>
            <MenuItem Header="Редактировать">
                <MenuItem Header="Очистить" Click="Clear_Click"/>
            </MenuItem>
            <MenuItem Header="Шрифт" Click="ChangeFont_Click"/>
        </Menu>
        <Grid Margin="0,25,0,0" VerticalAlignment="Stretch">
            <RichTextBox x:Name="docBox" HorizontalAlignment="Right" VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto"/>
        </Grid>
    </Grid>
</Window>
